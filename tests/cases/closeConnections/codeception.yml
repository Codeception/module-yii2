paths:
    tests: .
    log: ../../_output
    data: _data
    support: ../../_support
namespace: tests\
modules:
  config:
    Yii2:
      configFile: config.php
      transaction: false
      cleanup: true